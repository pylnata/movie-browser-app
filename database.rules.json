{
  "rules": {
      "reviews": {
		    ".read": "true",
    		".write": "true"
      },
      "watchlist": {
       "$uid": {
         ".read": "$uid === auth.uid",
         ".write": "$uid === auth.uid"
       }
    }
  }
}
